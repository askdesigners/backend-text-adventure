const Keystone = require("@keystonejs");
console.log(Keystone);

const Base = require("./Base.entity.js");

module.exports = class extends Base{
  constructor(id, name, description){
    super(id, );
  }
};